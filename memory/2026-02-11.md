# 2026-02-11 - Session Notes

## Configuration Changes

- **Primary model changed** to `openrouter/openrouter/aurora-alpha` (Aurora Alpha via OpenRouter).
- **Gateway restarted** to apply config changes.
- **Status Tracker issue:** Currently unreachable (Connection Refused at localhost:8000). Needs investigation.

## Brave-Scraper-MCP Audit & Improvements (2026-02-11)

### Manual Implementation:
- **Validation:** Added `minLength: 1` and `additionalProperties: False` to all tools in `src/server.py`.
- **New Feature:** Implemented `brave_scrape_page` tool for full Markdown extraction using `markdownify`.
- **Dependencies:** Added `markdownify` to `requirements.txt`.
- **Verification:** Created `tests/test_new_features.py` and verified routing and logic (4/4 tests passed).

### Project Context:
- User provided code snippets for a **Memory** system (including `MemoryType` enum, SQLAlchemy model, React UI components, and Pydantic models) for future reference/integration.
