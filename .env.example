# Status Tracker Environment Configuration
# Copy this file to .env and fill in your actual values

# Hostname for the application (used by Traefik)
# Example: myhost.local or status-tracker.example.com
HOST_NAME=your-hostname.local

# API Authentication Token (required for protected endpoints)
# Generate a secure token with: openssl rand -base64 32
# This token must be included in the Authorization header for API requests
API_AUTH_TOKEN=your-secure-api-token-here

# Optional: Database URL (defaults to sqlite:///./data/tasks.db)
# DATABASE_URL=sqlite:///./data/tasks.db

# Optional: Redis host (defaults to localhost)
# REDIS_HOST=localhost
